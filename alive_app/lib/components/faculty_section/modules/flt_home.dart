import 'package:flutter/material.dart';

class flt_home extends StatelessWidget {
  const flt_home({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
